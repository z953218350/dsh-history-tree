/**
 * dsh-history-tree — host half.
 * Clean, lightweight cordis plugin.
 */

export const name = 'history-tree'
export const inject = []

export function apply(ctx) {
  // Host-side lifecycle hooks if needed
}
